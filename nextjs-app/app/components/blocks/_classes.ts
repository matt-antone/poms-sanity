export enum BlockThemes {
  PRIMARY = 'theme-primary !bg-primary !text-primary-foreground',
  SECONDARY = 'theme-secondary !bg-secondary !text-secondary-foreground',
  ACCENT = 'theme-accent !bg-accent !text-accent-foreground',
  BACKGROUND = 'theme-background !bg-background !text-background-foreground',
  MUTED = 'theme-muted !bg-muted !text-muted-foreground',
  TRANSPARENT = 'theme-transparent !bg-transparent',
}

export enum TextAlign {
  left = 'text-left',
  center = 'text-center',
  right = 'text-right',
}

export enum GridCols {
  one = 'lg:grid-cols-1',
  two = 'lg:grid-cols-2',
  three = 'lg:grid-cols-3',
  four = 'lg:grid-cols-4',
}

export enum BentoGridCols {
  one = 'grid-cols-1',
  two = 'grid-cols-2',
  three = 'grid-cols-3',
  four = 'grid-cols-4',
}

export enum Colspan {
  one = 'lg:col-span-1',
  two = 'lg:col-span-2',
  three = 'lg:col-span-3',
  four = 'lg:col-span-4',
}

export enum VAlign {
  top = 'items-start',
  center = 'items-center',
  bottom = 'items-end',
}

export enum Justify {
  left = 'justify-start',
  center = 'justify-center',
  right = 'justify-end',
}